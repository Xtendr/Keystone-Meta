These fixtures are SYNTHETIC schema-equivalent samples for automated tests
and local visual inspection.

They are not live Raider.IO data. Do not present these numbers as real
Mythic+ representation, meta, or ranking values.

visual_snapshot.lua is a local-only populated layout fixture. Install it
with tools/use_visual_fixture.py. A file marked isSynthetic = true must
never be packaged or released.
